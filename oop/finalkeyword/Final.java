package finalkeyword;

// final key is app for classes, mtds, var
// if class is final we cannot extend the functionality of that class i.e inhritacne is not possible
// in final class all metds are by default final except var
// if mtd is final we cannot override that method
// final var should be initialized
// final var caanot be reaasigned
// instialize final -> at the time of declaration , inside instance block , or inside constructor
// instialize static final -> inside static block, at the time of declaration
// local var -> final or default 


public class Final {

}

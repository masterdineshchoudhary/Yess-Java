package multithreading;

// synchronized keyword is only applicable for methods and blocks but not for classes and variables
// if multiple threads are trying to operate on the same object then it can cause data inconsistency problem
// if a method or block is declared as synchronized, then at a time only one thread is allowed to perform its execution on the given object


public class Synchronization {
	
}

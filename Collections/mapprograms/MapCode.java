package mapprograms;

// MAP - If we want to represent a group of objects as a key-value pair
// key and value are objects
// duplicate keys are not allowed, values can be duplicated
// each key-value pair is called an entry. Hence, map is considered as collection of entry objects.

public class MapCode {
	public static void main(String[] args) {
		
	}
}
